<apex:page sidebar="false">
	<apex:canvasApp developerName="SpringCM_FileIt"
			width="100%"
			height="100%"
			parameters="{fUid:'{!$CurrentPage.parameters.fUid}',ldUid:'{!$CurrentPage.parameters.ldUid}',wfid:'{!$CurrentPage.parameters.wfid}',workflowinbox:'{!$CurrentPage.parameters.workflowinbox}',dlcname:'{!$CurrentPage.parameters.dlcname}',recordId:'{!$CurrentPage.parameters.sfoid}',oneclickdocgen:'{!$CurrentPage.parameters.oneclickdocgen}'}"
			scrolling="auto"/>
	<!--canvasApp height attr doesn't respect % values so this is basically a dummy value so it doesn't use the default 900px value-->
	<style type="text/css">
		iframe{
			height:100vh;
		}
	</style>
</apex:page>